import 'package:flutter/material.dart';

class calculatorTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Calculator Tab"),
    );
  }
}